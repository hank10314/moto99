{
  "name": "moto99",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf dist && mkdir -p dist && cp -R frontend/dist/. dist/",
    "dev": "npm --prefix frontend run dev",
    "preview": "npm --prefix frontend run preview"
  }
}
